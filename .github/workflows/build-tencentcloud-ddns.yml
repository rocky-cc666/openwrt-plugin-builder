name: Build TencentCloud DDNS APK

on:
  workflow_dispatch:

env:
  OPENWRT_VERSION: "25.12.5"
  PACKAGE_NAME: "luci-app-tencentddns"
  TENCENT_REPO: "https://github.com/Tencent-Cloud-Plugins/tencentcloud-openwrt-plugin-ddns.git"

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:

      # 1. 安装工具
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            clang \
            flex \
            bison \
            gawk \
            gettext \
            git \
            libncurses-dev \
            libssl-dev \
            python3 \
            rsync \
            unzip \
            wget \
            curl \
            file \
            zstd

      # 2. 下载 OpenWrt 25.12.5 SDK
      - name: Download OpenWrt SDK
        run: |
          ...

      # 3. 下载腾讯云官方 DDNS
      - name: Download TencentCloud DDNS
        run: |
          git clone \
            "$TENCENT_REPO" \
            tencentddns

      # 4. 自动修改 Makefile
      - name: Adapt package for OpenWrt 25.12.5
        run: |
          ...

      # 5. 放进 SDK
      - name: Install package
        run: |
          ...

      # 6. 编译 APK
      - name: Build APK
        run: |
          ...

      # 7. 验证 APK
      - name: Validate APK
        run: |
          ...

      # 8. 上传
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          ...

      # 9. Release
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          ...
